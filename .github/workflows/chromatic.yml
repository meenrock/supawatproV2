name: 'Chromatic'

on:
  push:
    branches: [ master ]
  pull request: 
    branches: [ master ]

  jobs:
    chromatic-deployment:

      runs-on: ubuntu-latest

      steps:
        - uses: actions/checkout@v2
          with: 
              fetch-depth: 0

              
      